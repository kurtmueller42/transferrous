# BUILDING

`cargo build`


# Running

Three executables are built:

* `transferrous-server` receives files
* `transferrous-client` sends files
* `transferrous-client-gui` is a gui that does nothing, yet